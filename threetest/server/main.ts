import { Meteor } from 'meteor/meteor';

import "/imports/games/simtower/api/publications"

import "/imports/meterm/TerminalSrv"

Meteor.startup(async () => {
  
  
//console.log(global["Meteor"]["server"])
  
});
